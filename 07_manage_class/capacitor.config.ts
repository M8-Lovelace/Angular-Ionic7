import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '07_manage_class',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
